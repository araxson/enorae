/**
 * Metric card component barrel export
 *
 * Provides unified metric card for business dashboards
 * with support for multiple variants (default, progress, trend, highlight)
 */
export { MetricCard } from './metric-card'
export type { MetricCardVariant } from './types'
export * from './utils'
