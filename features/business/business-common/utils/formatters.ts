// Re-export from value-formatters for backward compatibility
export {
  formatCurrency,
  formatPercentage,
  formatNumber,
  formatDuration,
  calculateGrowthRate,
  formatAnalyticsDate,
} from '../components/value-formatters'
