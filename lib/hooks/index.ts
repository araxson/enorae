/**
 * Custom React hooks barrel file
 * Exports all application-specific hooks
 *
 * Usage:
 *   import { useToast, useIsMobile } from '@/lib/hooks'
 */

export { useToast } from './use-toast'
export { useIsMobile } from './use-mobile'
