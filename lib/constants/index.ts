/**
 * Application Constants
 *
 * Centralized constants for the Enorae application.
 * Import from this file to maintain consistency across the app.
 *
 * Organization:
 * - app.ts: Core app metadata, branding, contact info
 * - config.ts: Business rules, validation, pagination, uploads
 * - routes.ts: Navigation links and portal routes
 * - statuses.ts: Status enums (appointments, staff, etc.)
 * - messages.ts: User-facing error and success messages
 */

// Core app constants
export * from './app'

// Configuration
export * from './config'

// Routes and navigation
export * from './routes'

// Status enums
export * from './statuses'

// Messages
export * from './messages'
