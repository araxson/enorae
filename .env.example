# ENORAE Platform - Environment Variables
# Copy this file to .env.local and fill in your actual values

# ============================================
# REQUIRED - Supabase Configuration
# ============================================

# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public key (safe to expose to client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (SERVER-SIDE ONLY - never expose to client)
# Required for admin operations and bypassing RLS when necessary
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# REQUIRED - Application URLs
# ============================================

# Public site URL (used for redirects, emails, etc.)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Alternative app URL (if different from site URL)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# OPTIONAL - External Services
# ============================================

# Google Maps API Key (for address autocomplete and maps)
# Get yours at: https://console.cloud.google.com/apis/credentials
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ============================================
# OPTIONAL - Environment Configuration
# ============================================

# Node environment (automatically set by Next.js)
# NODE_ENV=development

# Allow in-memory rate limiting (for development/testing only)
# Set to 'true' in development, 'false' or omit in production
# In production, use a distributed rate limiter (Redis, Upstash, etc.)
ALLOW_IN_MEMORY_RATE_LIMIT=true

# ============================================
# OPTIONAL - Feature Flags
# ============================================

# Enable/disable specific features
# FEATURE_LOYALTY_PROGRAM=false
# FEATURE_GIFT_CARDS=false
# FEATURE_MEMBERSHIP_PLANS=false

# ============================================
# NOTES
# ============================================

# 1. NEVER commit .env.local or any file with real credentials to git
# 2. All NEXT_PUBLIC_* variables are exposed to the browser
# 3. Server-only variables (without NEXT_PUBLIC_*) are never sent to client
# 4. Update this .env.example file when adding new required variables
# 5. For production, set these in your deployment platform (Vercel, Railway, etc.)
